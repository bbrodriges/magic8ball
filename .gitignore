# Ball works in mysterious ways
ball.go

# Binaries for programs and plugins
*.exe
*.exe~
*.so

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Build and deploy scripts
*.sh
